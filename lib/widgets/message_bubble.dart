import 'package:flutter/material.dart';

class MessageBubble extends StatelessWidget {
  const MessageBubble();

  @override
  Widget build(BuildContext context) {
    // TODO: Create the message bubble container
    return Container();
  }
}
